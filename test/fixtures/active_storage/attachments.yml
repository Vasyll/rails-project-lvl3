one_image:
  name: image
  record: one (Bulletin)
  blob: image_blob

two_image:
  name: image
  record: two (Bulletin)
  blob: image_blob

three_image:
  name: image
  record: under_moderation (Bulletin)
  blob: image_blob
